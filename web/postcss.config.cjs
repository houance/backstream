module.exports = {
    plugins: {
        'postcss-preset-mantine': {
            // Options can go here if needed, but defaults work fine
        },
        'postcss-simple-vars': {
            // You can define variables here, e.g., 'primary': '#00acee'
        },
    },
};
